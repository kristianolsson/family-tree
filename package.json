{
  "name": "family-tree",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "setup": "node scripts/setup.mjs",
    "sync": "node scripts/sync-template.mjs",
    "deploy:demo": "BASE_PATH=/family-tree vite build && gh-pages -d build --dotfiles",
    "prepare": "svelte-kit sync || echo ''"
  },
  "dependencies": {
    "family-chart": "^0.9.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.70.3",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/svelte": "^5.4.2",
    "gh-pages": "^6.3.0",
    "jsdom": "^25.0.0",
    "svelte": "^5.56.10",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "overrides": {
    "cookie": "^0.7.2"
  }
}
